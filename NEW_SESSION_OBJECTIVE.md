# codexShell 新会话开发目标

## 总目标

新建独立的 `codexShell` 桌面项目，先只实现 Codex 风格的桌面窗口外壳，不接入 OpsNest、SSH、AI、服务器数据或旧版存档。

## 第一阶段范围

1. 使用 Tauri + React + TypeScript 建立可运行的桌面项目。
2. 应用标题从 ChatGPT 改为 `co-shell`。
3. 暂时不放业务内容，只展示空的 UI 框架和必要的占位区域。
4. 实现 Codex 风格的四栏布局：
   - 左侧栏：可隐藏、可拖拽调整宽度；
   - 中间主区域：主内容区；
   - 右侧栏：可隐藏、可拖拽调整宽度；
   - 底部面板：可显示/隐藏、可拖拽调整高度。
5. 四栏之间使用平滑过渡动画；拖拽到最小阈值时吸附收起，并支持动画恢复。
6. 左侧栏切换按钮放在窗口最左上角，位置和 Codex 风格一致。
7. 右侧栏和底部面板各保留独立的折叠/展开控制。
8. 顶部窗口区域采用桌面应用布局，右上角保留标准最小化、最大化、关闭按钮；关闭按钮暂按项目后续规划处理，第一阶段至少保证窗口行为清晰。
9. 第一阶段优先还原布局比例、间距、字体层级、颜色、边界和动画质感，不追求业务功能。

## 明确不做

- 不迁移 V1/V2 业务代码。
- 不连接服务器。
- 不实现 SSH 终端。
- 不实现 Agent 或模型调用。
- 不实现文件管理。
- 不读取旧存档。
- 不打包 EXE，除非新会话完成 UI 骨架并通过基本构建检查后另行要求。

## 完成标准

- 新目录独立存在，结构清晰，后续可继续模块化扩展。
- 能启动桌面预览。
- 左、中、右、下四个区域均可按要求展开、收起和拖拽。
- 收起与展开有平滑动画，没有布局跳动、溢出或遮挡。
- 标题显示为 `co-shell`。
- `npm run check`、`npm run build` 通过；如果环境具备 Rust/Tauri 构建条件，再执行 Tauri 构建验证。
- 最终向用户展示一张实际运行截图或明确说明无法截图的原因。

## 新会话执行原则

先检查工具链和目标目录，再一次性搭建最小可运行骨架；不要碰 OpsNest 旧工程。完成 UI 骨架后再汇报文件位置、运行方式和检查结果。
